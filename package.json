{
  "name": "dose444",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "version": "1.0.0",
  "main": "build/plugin.mjs",
  "files":[
    "build"
  ],
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "bun build ./src/{components}/* --outdir ./dist",
    "builds": "bun build ./lib/{components,Layout}/* --outdir ./dist"
  },
  "dependencies": {
    "autoprefixer": "^10.4.17",
    "postcss": "^8.4.35",
    "tailwindcss": "^3.4.1"
  }
}